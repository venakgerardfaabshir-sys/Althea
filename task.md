# 背景
1、通信链路：S-FSK窄带链路，通信距离超远，灵敏度超高，速率为10kbps
2、灯控协议为DMX512协议，其通信速率要求不低于250kbps

# 解决思路
1、采用协议压缩技术，将原宽带协议中低频信息和协议头进行压缩，达到10倍的压缩率；

# 任务
1、分析DMX512协议，提取出协议的关键信息；
2、设计压缩算法，将关键信息进行压缩；
3、设计解压算法，将压缩后的信息进行解压；
4、设计接口，将压缩后的信息进行传输；
5、设计接口，将解压后的信息进行传输；

